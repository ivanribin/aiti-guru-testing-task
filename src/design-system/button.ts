import { ButtonPassThroughOptions } from "primereact/button";

const buttonDesignSystem: ButtonPassThroughOptions = {
    root: {
        className: `design-system-button description-text-primary`,
    },
};

export default buttonDesignSystem;
